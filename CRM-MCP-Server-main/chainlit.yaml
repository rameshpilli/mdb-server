chainlit_version: 1.0.0
port: 8501
host: localhost

[features]
[features.mcp.sse]
    enabled = true
    allowed_origins = ["http://localhost:8000"]

[features.mcp.stdio]
    enabled = true
    allowed_executables = ["python"]

[ui]
name = "MCP Chat Interface"
description = "A Model Context Protocol (MCP) chat interface with intelligent routing and tool execution"
theme = "light"
favicon = "🤖" 